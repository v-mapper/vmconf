{
    "vm: enable vmconf develop":
    [
        {
            "TYPE": "jobType.PASSTHROUGH",
            "SYNTAX": "su -c 'mount -o remount,rw /system && /system/bin/curl -L -o /system/etc/init.d/55vmapper -k -s https://raw.githubusercontent.com/v-mapper/vmconf/removePDdependencies/55vmapper && chmod +x /system/etc/init.d/55vmapper && mount -o remount,ro /system && touch /sdcard/useremovePDdependencies && /system/etc/init.d/55vmapper'"
        }
    ],

    "vm: disable vmconf develop":
    [
        {
            "TYPE": "jobType.PASSTHROUGH",
            "SYNTAX": "su -c 'mount -o remount,rw /system && /system/bin/curl -L -o /system/etc/init.d/55vmapper -k -s https://raw.githubusercontent.com/v-mapper/vmconf/main/55vmapper && chmod +x /system/etc/init.d/55vmapper && mount -o remount,ro /system && rm -f /sdcard/useremovePDdependencies && /system/etc/init.d/55vmapper'"
        }
    ]
}
